<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HSESupporter.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <Label HorizontalTextAlignment="Center"
                   Text="HSE Supporter"
                   FontSize="30"
                   FontAttributes="Bold"
                   TextColor="#5B69FF" />
            <Entry x:Name="Login" Placeholder="Логин" Margin="12,0" />
            <Entry x:Name="Password" Placeholder="Пароль" IsPassword="True" Margin="12,0" />
            <Button Text="Войти" FontSize="22" Clicked="Button_OnClicked" />
            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="False" />
            <Label x:Name="ResultText" HorizontalTextAlignment="Center"
                   Text="Успешно"
                   FontSize="20"
                   IsVisible="False"
                   FontAttributes="Bold"
                   TextColor="#5B69FF" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>